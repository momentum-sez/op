[package]
name = "op-lex-compiler"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Compilation function [[·]] : Lex -> Op, as specified in the Op paper §6.2."

[dependencies]
op-core = { path = "../op-core" }
op-compiler = { path = "../op-compiler" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "compile_lex"
harness = false
